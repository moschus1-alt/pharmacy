# 약사 생존기 작업 규칙

## 캐릭터 에셋

- 캐릭터 이미지를 생성·교체·수정하기 전에 `docs/CHARACTER_SPRITE_PIPELINE.md`를 전부 읽고 따른다.
- 활성 캐릭터 에셋은 `assets/characters-v2/`에 있다.
- 보행 시트는 512×512 RGBA, 4×4, 셀 128×128이다.
- 행은 정면/왼쪽/오른쪽/뒷면, 열은 기본/왼발/통과/오른발 순서다.
- 오른쪽 행은 왼쪽 행의 정확한 수평 반전이어야 한다.
- 배경이 투명해 보이는 것만으로 통과시키지 말고 알파 채널을 검사한다.
- 16개 프레임, 투명도, 중심, 발 기준선, 좌우 반전, 잘림을 검증한 뒤에만 게임 경로를 바꾼다.
- 민준과 서연 초상은 같은 캐릭터 스타일 시트에서 만든 이미지를 사용한다.

## 게임 방향 매핑

- 아래쪽 이동은 row 0, 왼쪽은 row 1, 오른쪽은 row 2, 위쪽은 row 3이다.
- 캐릭터별 임시 좌우 반전이나 예외 분기를 추가하지 않는다.
- 모든 플레이어와 적은 `Utils.directionRow`와 `Utils.walkFrame`을 공통 사용한다.

## 검증

- 변경 후 브라우저에서 민준·서연과 모든 적의 네 방향 이동을 확인한다.
- 이미지 로드 실패, 콘솔 오류, 투명 배경 사각형, 셀 경계 잘림이 없어야 한다.
- 기존 사용자 변경을 보존하고, 검증 전에는 이전 에셋을 삭제하지 않는다.
